# Dependencies
node_modules/

# Build output
dist/
build/
*.tsbuildinfo

# npm lockfile — the Dockerfile uses `npm install`, so no lockfile is committed
# (consistent across all phases).
package-lock.json

# Environment (never commit real secrets; .env.example is the template)
.env
.env.local
*.local

# Logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# OS / editor cruft
.DS_Store
.vscode/
.idea/

# Source datasets — large / not for version control.
# The 400_transactions.csv alone is ~128 MB. Phase 2 commits a trimmed sample
# under data/sample/ instead.
project-breif/data-sources/*.csv

# Prisma local artifacts
*.db
